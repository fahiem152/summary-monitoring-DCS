import 'package:flutter/material.dart';

class MonitoringSM extends StatelessWidget {
  const MonitoringSM({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Ini Halaman Monitoring Stock Material"),
    );
  }
}
