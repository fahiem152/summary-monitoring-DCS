import 'package:flutter/material.dart';

class SumNGPart extends StatelessWidget {
  const SumNGPart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Ini Halaman Summary NG Part"),
    );
  }
}
