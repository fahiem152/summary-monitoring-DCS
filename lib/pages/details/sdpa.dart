import 'package:flutter/material.dart';

class SumDailyProdAchiev extends StatelessWidget {
  const SumDailyProdAchiev({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Ini Halaman Summary Dayly Production Achievement"),
    );
  }
}
