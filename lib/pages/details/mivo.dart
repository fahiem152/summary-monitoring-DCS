import 'package:flutter/material.dart';

class MaterialInVSOut extends StatelessWidget {
  const MaterialInVSOut({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Ini Halaman Material In VS Out"),
    );
  }
}
